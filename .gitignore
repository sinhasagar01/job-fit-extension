# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.output
stats.html
stats-*.json
.wxt
web-ext.config.ts

# Build artifacts (e.g. `npm run zip` output)
*.zip

# Throwaway mock eval baseline (real gemini/groq baselines are committed)
eval/baselines/mock.json

# Quarantined invalid baselines (complete:false runs) — never commit these
eval/baselines/.incomplete/

# Cloudflare Worker — local state, secrets, build output
worker/.wrangler/
worker/.dev.vars
worker/dist/

# Local-only docs (not part of the tracked project)
ARCHITECTURE.md
AGENTS.md
JobFit-KEY-FACTS.md

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
worker/.dev.vars
